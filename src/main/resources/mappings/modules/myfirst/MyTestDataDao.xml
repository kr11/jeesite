<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.myfirst.dao.MyTestDataDao">
    
	<sql id="myTestDataColumns">
		a.id AS "id",
		a.user_id AS "user.id",
		a.user_name AS "userName",
		a.sex AS "sex",
		a.standard AS "standard",
		a.in_date AS "inDate",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		u2.name AS "user.name"
	</sql>
	
	<sql id="myTestDataJoins">
		LEFT JOIN sys_user u2 ON u2.id = a.user_id
	</sql>
    
	<select id="get" resultType="MyTestData">
		SELECT 
			<include refid="myTestDataColumns"/>
		FROM my_test_data a
		<include refid="myTestDataJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="MyTestData">
		SELECT 
			<include refid="myTestDataColumns"/>
		FROM my_test_data a
		<include refid="myTestDataJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="user != null and user.id != null and user.id != ''">
				AND a.user_id = #{user.id}
			</if>
			<if test="userName != null and userName != ''">
				AND a.user_name = #{userName}
			</if>
			<if test="sex != null and sex != ''">
				AND a.sex = #{sex}
			</if>
			<if test="standard != null and standard != ''">
				AND a.standard = #{standard}
			</if>
			<if test="beginInDate != null and endInDate != null and beginInDate != '' and endInDate != ''">
				AND a.in_date BETWEEN #{beginInDate} AND #{endInDate}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="MyTestData">
		SELECT 
			<include refid="myTestDataColumns"/>
		FROM my_test_data a
		<include refid="myTestDataJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO my_test_data(
			id,
			user_id,
			user_name,
			sex,
			standard,
			in_date,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{user.id},
			#{userName},
			#{sex},
			#{standard},
			#{inDate},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE my_test_data SET 	
			user_id = #{user.id},
			user_name = #{userName},
			sex = #{sex},
			standard = #{standard},
			in_date = #{inDate},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE my_test_data SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>